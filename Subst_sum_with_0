 static boolean findsum(int arr[],int n)
    {
        int sum=0;
        Set<Integer> s=new HashSet<>();
       for(int i=0;i<n;i++)
       {
           s.add(sum);
           sum=sum+arr[i];
          if(s.contains(sum))
          return true;
       }
       return false;
    }
